version: '3.8'

services:
  dharma-sikhara-website:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./subscribers.json:/app/subscribers.json
    environment:
      - NODE_ENV=production
      - PORT=3001
      - HOST=0.0.0.0
    restart: unless-stopped